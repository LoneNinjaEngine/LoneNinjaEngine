###################################################################################

> kindly install python 3.4 and NLTK package add on for python.

> Ensure that the environment variables are set to the python installation directory.

> Place the input text file as "inputfile.txt" parallel to the project files.

> To run the Engine, Trigger the file "Execute_LoneNinjaEngine.bat"

> Choose  "n" when prompted to load the feed

> Results will be available in "outputfile.txt"